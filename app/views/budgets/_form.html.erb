<%= form_for(@budget) do |f| %>
  <% if @budget.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@budget.errors.count, "error") %> prohibited this budget from being saved:</h2>

      <ul>
      <% @budget.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
        <%= f.label :title %><br />
        <%= f.text_field :title %>
    </div>
    <div class="field">
        <%= f.label :beginning_balance %><br/>
        <%= f.text_field :beginning_balance %>
    </div>
    <div class="field">
        <%= f.label :period_start_date %><br/>
        <%= f.text_field :period_start_date %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
